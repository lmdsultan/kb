<p id="notice"><%= notice %></p>

<h1>Systems</h1>

<table id="t01">
  <thead>
    <tr>
      <th>System</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <div class="btn-group">
  <tbody>
    <% @systems.each do |system| %>
      <tr>
        <td><%= system.name %></td>
        <td><%= link_to 'All incidents', system , :class => "btn"%></td>'
      </tr>
    <% end %>
  </tbody>
  </div>
</table>


<% 
=begin %>
 <td><%= link_to 'Edit', edit_system_path(system) %></td>
 <td><%= link_to 'Destroy', system, method: :delete, data: { confirm: 'Are you sure?' } %></td>
<% 
=end %>


<br>

<%= link_to 'New System', new_system_path , :class => "btn" %>
